<Window x:Class="MedicineProject.AddVisitWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MedicineProject"
        mc:Ignorable="d"
         Title="Новый визит" Height="350" Width="450"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Дата визита:"/>
        <DatePicker x:Name="dpDate" Grid.Row="0" Margin="150 0 0 5"/>

        <TextBlock Text="Диагноз:" Grid.Row="1" Margin="0 10 0 0"/>
        <TextBox x:Name="txtDiagnosis" Grid.Row="1" Margin="150 10 0 0"/>

        <TextBlock Text="Назначения:" Grid.Row="2" Margin="0 10 0 0"/>
        <TextBox x:Name="txtPrescriptions" Grid.Row="2" Margin="150 10 0 0"/>

        <TextBlock Text="Тип направления:" Grid.Row="3" Margin="0 10 0 0"/>
        <ComboBox x:Name="cbReferralType"
                  Grid.Row="3"
                  Margin="150 10 0 0"
                  ItemsSource="{Binding ReferralTypes}"/>

        <Button Content="Создать"
                Grid.Row="4"
                Width="150"
                Height="40"
                HorizontalAlignment="Left"
                Margin="0 20 0 0"
                Background="#3A8DDF"
                Foreground="White"
                Click="BtnCreate_Click"/>
    </Grid>
</Window>