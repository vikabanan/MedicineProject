<Window x:Class="MedicineProject.AddReferralWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MedicineProject"
        mc:Ignorable="d"
        Title="Добавить направление" Height="350" Width="400"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Дата:" Grid.Row="0" Margin="0,0,0,5"/>
        <DatePicker x:Name="dpDate" Grid.Row="1" Margin="0,0,0,10"
                    SelectedDateChanged="DpDate_SelectedDateChanged"/>

        <TextBlock Text="Тип направления:" Grid.Row="2" Margin="0,0,0,5"/>
        <ComboBox x:Name="cbType" Grid.Row="3" Margin="0,0,0,10" SelectionChanged="CbType_SelectionChanged"/>

        <TextBlock Text="Время:" Grid.Row="4" Margin="0,0,0,5"/>
        <ComboBox x:Name="cbTime" Grid.Row="5" Width="120" Margin="0,0,0,10"/>

        <Button Content="Создать"
                Grid.Row="6"
                Width="130"
                Height="40"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Background="#3A8DDF"
                Foreground="White"
                Click="BtnCreateReferral_Click"/>
    </Grid>
</Window>